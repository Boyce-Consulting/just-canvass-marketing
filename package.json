{
  "name": "justcanvass-app-marketing",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html --open",
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url ./ && cp src/CNAME dist/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel-bundler": "^1.12.5",
    "sass": "^1.85.1"
  }
}
